# TIPS 开发记录

> Alan's Future Plan — 个人专属 AI 升学规划系统
> 最后更新: 2026-06-01

---

## 项目概况

- **目标用户**: Alan（江苏扬州扬大附中高一学生，2028高考）
- **AI 助手**: TIPS（原名知衡）
- **技术栈**: 纯前端 PWA + DeepSeek API + Bing Search API + IndexedDB + Chart.js
- **代码总量**: 2,241 行（核心 JS 1,636 行）
- **部署方式**: GitHub Pages（HTTPS）

---

## 版本历史

### v0.5 — 架构重构（已交付）
**目标**: 为所有后续功能打好数据层和代码结构基础

| 交付物 | 说明 |
|--------|------|
| `db.js` | IndexedDB 封装层，8 个存储对象（settings/conversations/grades/reports/memory/search_cache/usage_log/knowledge_index） |
| `prompts.js` | Agent 提示词从 app.js 拆分独立管理 |
| 旧数据迁移 | localStorage → IndexedDB 自动迁移 |
| Tab 导航 | 底部 4 Tab（对话/学情/报告/我的） |
| `memory/index.json` | 知识新鲜度索引，7 条知识条目 + 过期规则 |
| `memory/` 目录结构 | knowledge/policy, universities, majors, industry + profile + conversations + exports |
| Token 记录 | callDS() 每次调用记录用量到 usage_log |
| 改名 TIPS | 从「知衡」全面更名 |

### v1.0 — MVP 核心功能（已交付）
**目标**: PAD 端可日常使用的完整应用

| 交付物 | 说明 |
|--------|------|
| `charts.js` | Chart.js v4 六种图表（趋势折线/雷达/总分/波动柱状/用量趋势/模型分布） |
| 全链路流水线 | A2(学情)→A4(产业)→A3(策略)→A4(院校)→A1(总控)，带进度条 |
| Agent4 提示词 | P4I 产业趋势 + P4M 专业院校完整实现 |
| 学情 Tab | 科目卡片网格（分数/均分/趋势/档位）+ 4 张图表 |
| 我的 Tab | 个人信息/统计/记忆状态/设置入口 |
| 波动检测 | CSV 导入后自动检查 >=15% 波动，聊天推送告警 |
| Markdown 渲染 | 表格/代码块/引用/列表完整渲染，聊天消息也支持 |

### v1.5 — 增强功能（已交付）
**目标**: 接入外部能力 + 知识闭环

| 交付物 | 说明 |
|--------|------|
| `manifest.json` + `sw.js` | PWA 支持，可添加到 PAD 桌面，离线缓存 |
| 拍照上传识别 | 成绩单拍照 → DeepSeek Vision → 识别 → 可编辑确认 → 保存 |
| Bing 搜索 | 消息前加 🔍 触发联网搜索，结果缓存 24h，DeepSeek 解读后回复 |
| GitHub Pages 部署 | `.github/workflows/deploy.yml` 自动构建 + 部署 |
| 知识同步 | PAD 启动时自动拉取 GitHub Pages 上的 `memory-bundle.json` |
| 家长面板 | 4 位 PIN 锁定，用量仪表板/预算设置/数据导出导入清除 |
| 预算控制 | 月 Token 90% 警告，100% 自动切 Flash 模型 |

### v2.3 — 性能与体验优化（已交付）
**目标**: 全链路分析加速、缓存机制、报告管理

| 交付物 | 说明 |
|--------|------|
| 分析缓存 | 成绩不变直显示上次报告，秒开 |
| 超时优化 | 180s + 安全超时 10min |
| A1/A4 模型调优 | 按需分配 Flash/Pro 机型 |
| 报告列表+导出 | 历史报告切换、导出 .md |
| P0 专业引导 | 聊天时自然引出专业/城市/就业 |
| 缓存 key 修复 | 拍照/改分等所有成绩变更刷新缓存 |
| 齿轮按钮修复 | 误删函数恢复 |

### v2.0 — 生产准备（已交付）
**目标**: 安全加固 + 家长视角 + 性能优化

| 交付物 | 说明 |
|--------|------|
| `crypto.js` | Web Crypto API AES-GCM 加密，非默认 PIN 时自动加密 API Key |
| 双视角 | 设置中「家长模式」开关，关闭时孩子看不到家长面板入口 |
| 性能优化 | requestAnimationFrame 双缓冲图表渲染，防重复渲染锁 |

---

## 文件清单

```
d:/AI/Alan's Future Plan/
├── index.html             483行  主界面，内嵌 CSS + HTML 结构
├── app.js                 922行  核心应用逻辑（聊天/分析/Tab/搜索/家长面板）
├── db.js                  341行  IndexedDB 封装层（8 store + CRUD + 便捷方法）
├── prompts.js              61行  5 个 Agent 系统提示词
├── charts.js              266行  Chart.js v4 六种图表封装
├── crypto.js               47行  Web Crypto API AES-GCM 加密
├── sw.js                   60行  Service Worker（缓存优先 + 离线）
├── manifest.json           14行  PWA 清单
├── agent2.py                —    备用离线学情分析脚本（Python）
├── .gitignore               —    Git 忽略规则
├── .github/workflows/
│   └── deploy.yml          48行  GitHub Actions 自动部署
├── icons/
│   └── icon.svg             —    PWA 图标
├── memory/
│   ├── index.json          84行  知识新鲜度索引
│   ├── MEMORY.md            —    记忆总索引
│   ├── knowledge/           —    政策/院校/专业/产业知识库
│   │   ├── policy/
│   │   ├── universities/
│   │   ├── majors/
│   │   └── industry/
│   ├── profile/             —    孩子画像（学科轨迹/兴趣/情绪/里程碑）
│   ├── conversations/       —    聊天归档（PC端）
│   ├── exports/             —    PC 同步知识包
│   ├── references/          —    参考资料
│   └── backups/             —    CSV 备份
├── reports/                 —    Agent1 生成的历史报告
├── conversation_log.md      —    对话摘要记录
├── 成绩20260529.csv         —    成绩数据
└── 开发记录.md              —    本文件
```

---

## 架构概要

```
PAD 浏览器 (PWA)
  │
  ├── IndexedDB (TIPS_DB)
  │   ├── settings        键值配置
  │   ├── conversations   聊天消息
  │   ├── grades          成绩（每科目一条）
  │   ├── reports         分析报告
  │   ├── memory          系统记忆
  │   ├── search_cache    搜索缓存 (24h TTL)
  │   ├── usage_log       Token 用量日志
  │   └── knowledge_index 知识库索引
  │
  ├── DeepSeek API      对话 + 分析 + 视觉识别
  ├── Bing Search API   联网搜索（可选配置）
  └── GitHub Pages      知识同步源
```

### Agent 流水线

```
CSV/图片 → A2(学情) → A4(产业趋势) → A3(组合策略) → A4(专业院校) → A1(总控报告)
                                           ↑                    ↑
                                     Bing 搜索补充     知识库补充
```

---

## 关键设计决策

1. **Agent 是逻辑层**，非独立服务，在 JS 运行时内串行调用，数据以纯对象传递
2. **对话隔离**：日常聊天只带记忆摘要（几百字），全量数据只在分析管道中使用
3. **知识新鲜度驱动**：每条知识带 fetched_at + expires_at，过期自动搜索刷新
4. **PC→PAD 单向同步**：家长在 PC 用 Claude 搜索整理 → git push → PAD 启动时自动拉取
5. **grades 存储设计**：每科目一条记录（非 CSV 行），按 subject 索引可快速查单科历史曲线

---

## 待办 / 未来可扩展

- [x] 配置实机部署（GitHub Pages 已上线）
- [x] 首次搜索高考政策 + 院校录取数据
- [ ] 首次搜索高考政策 + 院校录取数据，填充 knowledge/ 目录
- [ ] 高二分班后获取正式年排，运行首次全链路分析
- [ ] 多设备同步机制（PAD 端数据回传 PC）
- [ ] 情绪记录可视化（趋势图）
- [ ] 推送通知（考试提醒）
- [ ] 跨设备同步（PAD → PC）
